import { useEffect,useRef } from "react";
import type { Incident,IncidentStatus } from "@/components/control/controlTypes";
interface Props{incident:Incident|null;open:boolean;onClose:()=>void;onClaim:()=>void;onConfirm:()=>void;onFalsePositive:()=>void;onDispatchRequest:()=>void;onCctvClick:(id:string)=>void}
const labels:Record<IncidentStatus,string>={new:"신규 탐지",claimed:"선점됨",confirmed:"실제 위험",falsePositive:"오탐",dispatchRequested:"출동 요청됨",dispatched:"출동 진행 중",resolved:"조치 완료"};
export function IncidentDetailPanel({incident,open,onClose,onClaim,onConfirm,onFalsePositive,onDispatchRequest,onCctvClick}:Props){const closeRef=useRef<HTMLButtonElement>(null);useEffect(()=>{if(open)closeRef.current?.focus()},[open]);if(!open||!incident)return null;const canClaim=incident.status==="new";const canJudge=incident.status==="claimed";const canDispatch=incident.status==="confirmed";return <div className="detail-overlay" onClick={onClose} role="dialog" aria-modal="true" aria-labelledby="incident-detail-title"><aside className="detail-panel" onClick={e=>e.stopPropagation()}><div className="detail-panel__header"><div><p className="detail-panel__label">INCIDENT DETAIL</p><h2 id="incident-detail-title">{incident.number}</h2></div><button ref={closeRef} type="button" className="detail-panel__close" onClick={onClose} aria-label="사건 상세 닫기">×</button></div><div className="detail-panel__summary"><span className={`status-pill status-pill--${incident.severity==="긴급"?"danger":incident.severity==="주의"?"warning":"review"}`}>{labels[incident.status]}</span><span className={`status-pill status-pill--${incident.risk>=90?"danger":incident.risk>=80?"warning":"review"}`}>{incident.riskLabel}</span></div><div className="detail-panel__grid"><div><p>객체 유형</p><strong>{incident.type}</strong></div><div><p>도로명</p><strong>{incident.road}</strong></div><div><p>진행 방향</p><strong>{incident.direction}</strong></div><div><p>구간 / 거리</p><strong>{incident.segment}</strong></div><div><p>연결 CCTV</p><strong><button type="button" className="detail-panel__cctv-link" onClick={()=>onCctvClick(incident.cctvId)}>{incident.cctvId}</button></strong></div><div><p>AI 탐지 근거</p><strong>{incident.evidence}</strong></div><div><p>최초 탐지 시간</p><strong>{incident.detectedAt}</strong></div><div><p>대기 시간</p><strong>{incident.waiting}</strong></div><div><p>담당자</p><strong>{incident.assignedTo??"미배정"}</strong></div><div><p>출동 요청</p><strong>{incident.dispatchRequested?"요청됨":"미요청"}</strong></div></div><div className="detail-panel__video"><div className="detail-panel__video-label">탐지 근거 영상</div><div className="detail-panel__video-placeholder">AI CCTV EVIDENCE</div></div><div className="detail-panel__actions"><button type="button" onClick={onClaim} disabled={!canClaim}>사건 선점</button><button type="button" onClick={onConfirm} disabled={!canJudge}>실제 위험</button><button type="button" onClick={onFalsePositive} disabled={!canJudge}>오탐 처리</button><button type="button" onClick={onDispatchRequest} disabled={!canDispatch||incident.dispatchRequested}>출동 요청</button></div></aside></div>}
